set result 0

function porraloca
    set a 0
    while a < 100
        set a a + 1

    while a > 95
        show wow! a
        set a a-1
        

    result a

execute porraloca
apply result
show `result`: result
